name: cron-settle
on:
  schedule:
    - cron: "*/12 * * * *"   # UTC — settle finished games
  workflow_dispatch: {}
jobs:
  hit:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -m 120 "https://${{ secrets.VOLTBET_APP_URL }}/api/cron/settle?secret=${{ secrets.VOLTBET_CRON_SECRET }}"
